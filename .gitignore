# Node
node_modules/

# TypeScript incremental build cache
tsconfig.tsbuildinfo
*.tsbuildinfo

# Build artifacts
dist/

# Rust / tauri
target/
src-tauri/target/

# Cargo.lock (author's choice — not committed in this tauri app)
*.lock
